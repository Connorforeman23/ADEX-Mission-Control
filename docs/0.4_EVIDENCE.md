# Package 0.4 — Safe operating and release controls — evidence

Objective: make production changes deliberate and reversible, and make dev
disposable and safe. Settings and process only — no production schema/data change.

## Acceptance criteria

| Criterion | Owner | Status |
|---|---|---|
| Dev can be reset and re-seeded with fictional data | Claude (built) + Connor (run) | ✅ `supabase/seed/dev_seed.sql` written |
| Seed refuses to run against production | Claude | ✅ guard: aborts if a Randox client exists |
| Dev cannot send live emails / POs / Xero / webhooks | Claude (verified) | ✅ none exist; recorded in OPERATIONS.md §2 |
| `main` (production) branch is protected — PR + Rick approval | Connor (GitHub) | ✅ ruleset "Main" Active, targets `main`, requires PR (approvals 0 for now) |
| Release process documented | Claude | ✅ OPERATIONS.md §3 |
| Rollback route documented and available | Claude | ✅ OPERATIONS.md §5 (Vercel + git revert) |
| Daily backups confirmed to exist | Connor (Supabase) | ✅ 6 daily physical backups, latest 10 Aug 2026 05:57 (11 Aug 2026). Note: Storage objects not included (no uploads in use yet) |
| Restore into a temporary project tested | Connor (Supabase) | ⚠️ Skipped by decision — safe "restore to new project" route not cleanly available in UI; in-place restore exists but not drilled on live. Backups confirmed present; residual risk accepted (11 Aug 2026) |
| Rick can test previews | — | ✅ Not needed — preview links are plain URLs Rick opens in a browser; no Vercel seat required unless Deployment Protection is enabled (it need not be). Enterprise plan NOT required (11 Aug 2026) |
| Team can log into dev (auto-confirmed user) | Connor (Supabase) | ✅ Connor logged into dev preview, DEV banner + seed data visible (11 Aug 2026) |

## Evidence log

_(fill in as each item completes)_

- Dev seeded on: 11 Aug 2026  (result: 4 clients, 5 campaigns, 6 lines, 4 leads, 3 tasks; prod guard not tripped)
- Backups confirmed on: 11 Aug 2026  (6 daily physical backups, latest 10 Aug 05:57; PITR add-on declined per D2)
- Restore test done on: SKIPPED (11 Aug 2026) — decision to accept residual risk; revisit if data volume/criticality grows
- Branch protection enabled on: 11 Aug 2026  (ruleset "Main", Active, requires PR to merge to `main`)
- Rick's Vercel access: not required — Rick tests via preview URLs in a browser, no seat/plan change
