# Package 1.5 — evidence

## Dev test plan
1. Apply `0005_audit_offboarding.sql` on dev (in blocks — see chat).
2. **Audit:** as admin, create/edit a campaign or opportunity → Settings →
   "Activity log" shows the change (who, when, what changed).
3. **Offboarding — disable:** in "Manage team", Disable the restricted test user →
   log in as them → immediately bounced to /login with "access has been removed".
   Re-enable → they can log in again.
4. **Last-admin safeguard:** try to Disable your own (only) admin → refused.

## Dev verification
_(to complete)_
- 0005 applied on dev: __________
- Audit log recorded a change: __________
- Disable locked the user out on next click: __________
- Re-enable restored access: __________

## Production cut-over
_(to complete)_
- 0005 applied on prod (in blocks): __________
- Merged / redeployed: __________
- Live check (activity log populates; disable/enable works): __________
