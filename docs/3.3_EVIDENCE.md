# Phase 3.3a — organisations structure — evidence

Migration `0006_organisations.sql` applied to **dev**, 11 Aug 2026. Additive and
parallel-running: nothing dropped, no existing column altered, app unaffected.

## Result on dev — PASS
12 organisations correctly derived from 4 clients + 4 leads + 4 supplier vendors:

| Organisation | customer_status | is_supplier | Derived from |
|---|---|---|---|
| Acme Test Client | active_client | false | client (live) |
| Example Media Co | active_client | false | client (live) |
| Sample Care Group | former_client | false | client (hold) |
| Test Leisure Ltd | prospect | false | client (plan) |
| Prospect One Ltd | prospect | false | lead (Engaged) |
| Prospect Two Ltd | prospect | false | lead (Proposal) |
| Won Deal Co | active_client | false | lead (Closed Won) |
| Lost Deal Co | not_pursuing | false | lead (Closed Lost) |
| FT / Global / ITV / JCD | none | true | campaign_lines.vendor |

Notes:
- Meta does not appear as a supplier because its only booking line belonged to
  TST-0004, deleted during the 1.4 delete-guard test. Correct, not a defect.
- Status mapping agreed 11 Aug: live→active_client, hold→former_client,
  plan→prospect; Closed Won→active_client, Closed Lost→not_pursuing, else prospect.
- The two genuinely new lifecycle states (former_client, not_pursuing) are now
  representable for the first time.

## Still to do
- **3.3b — app switch-over**: screens still read the old `clients`/`leads` tables.
  The new columns (`client_org_id`, `supplier_org_id`, `contacts.organisation_id`)
  are populated and ready but unused by the UI.
- **3.4 — retire** the old tables and re-point the remaining RLS policies once
  the app no longer reads them; add organisation rows to SECURITY_SELFCHECK.
- Production: apply 0006 only after 3.3b is proven on dev (schema-before-app rule
  from the release checklist still applies to each step).
