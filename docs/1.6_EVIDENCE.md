# Package 1.6 — Security regression and controlled release — evidence

The closing package: prove every control from 1.1–1.5 is present and cannot
silently regress, and put the release process in writing.

## Deliverables
- `supabase/SECURITY_SELFCHECK.sql` — one read-only query verifying every control
  (1.1 role guard, 1.2 admin/signup locks, 1.3 access model, 1.4 integrity
  constraints + delete guard, 1.5 audit + offboarding, RLS on sensitive tables).
  Every row must read PASS. Re-runnable any time.
- `docs/RELEASE_CHECKLIST.md` — the standing release process, including the
  "database to prod before merging the app PR" rule learned in 1.5.

## Verification
_(to complete)_
- SECURITY_SELFCHECK.sql on production — all PASS: __________
- SECURITY_SELFCHECK.sql on dev — all PASS: __________

## Programme close
On all-PASS, packages 0.1–0.4 and 1.1–1.6 are complete. See
`IMPLEMENTATION_PLAN.md` for the full status and `docs/` for each package's
assessment and evidence.
