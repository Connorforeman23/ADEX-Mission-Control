# Package 0.3 — evidence

Verified 3 August 2026 on the `phase-0.3-env-separation` branch.

## Proven on preview

| Criterion | Result |
|---|---|
| Missing variables fail clearly | ✅ guard throws "Missing required environment variable" (build test) |
| Environment/project mismatch refuses to start | ✅ production label + dev URL → "Environment mismatch — refusing to start" (build test) |
| Matching pairs build and run | ✅ dev preview builds green and loads |
| Preview uses the dev database only | ✅ preview deployment points at dev (ficmtwvmmcsxxexhysbd) |
| Local development uses dev only | ✅ .env.local set to dev |
| No hard-coded fallback remains | ✅ removed from client, server, proxy; config from env only |
| Dev/test banner on non-production | ✅ orange banner shows on every page incl. login, on the preview |

## A real bug caught on preview, not production

The first preview failed to load. Cause: the guard read `NEXT_PUBLIC_*`
variables through a dynamic helper, which Next.js cannot inline, so the edge
runtime (middleware) saw them as empty and refused every request. Fixed by
reading them by their literal names. **This would have taken the live app
down for the whole team had it been pushed straight to production** — instead
it surfaced on a preview against the dev database, which is exactly what the
dev/preview split exists for.

## Production cut-over — COMPLETE (3 August 2026)

Merged to main (commit dfd9316), production redeployed. Verified:

| Check | Result |
|---|---|
| Prod anon key validated against the live project before merge | ✅ 200 on prod, 401 on dev |
| Branch contained only 0.3 changes | ✅ 4 commits, env-separation only |
| Rollback target identified | ✅ 6c1fd99, Vercel instant rollback + git revert |
| Live site starts, no error | ✅ /login 200, / redirects to login |
| No DEV banner on production | ✅ 0 occurrences |
| Production bundle bound to the prod project | ✅ references only puolnlqp…, never dev |
| Sign-in reaches the production database | ✅ "Invalid login credentials", not "Invalid API key" |
| Normal reads (real data loads) | ✅ confirmed by Connor logged in |

No rollback needed. Package 0.3 complete.

---

## (historical) Still outstanding for 0.3 completion (production cut-over)

The code is proven on preview but **not yet deployed to production**. The final
step merges the branch to `main`, which redeploys production on the new
env-guarded code. Before that:

1. **Re-confirm the Production-scope variables** with known-correct values —
   the production anon key has been suspect since the 29 July mis-scope that
   led to the committed fallback in the first place. Removing the fallback
   means production must have a correct key, or sign-in breaks.
2. **Rick's approval** — required for any production deployment.
3. **Deploy at a quiet time, with rollback ready** — Vercel keeps the previous
   (fallback) deployment; a bad cut-over is a one-click rollback, no data risk.

This step is deliberately held. Claude will not merge to `main` without the
above.
