# Package 1.2 — evidence

## Password policy hardening (dashboard)
- Min length 8, breached-password rejection ON, OTP expiry 15 min.
- Applied on dev by Connor; **production to confirm at cut-over**. Existing
  logins unaffected — only new/changed passwords are checked.

## Admin panel — proven on dev (11 Aug 2026)
Verified on the `phase-1.2-auth-admin` preview against the dev database:

| Test | Result |
|---|---|
| "Manage team" card visible to admin | ✅ |
| Add a staff-list entry | ✅ appears in list |
| Change a role via dropdown | ✅ saves |
| Remove a staff-list entry | ✅ removed |
| Last-administrator safeguard | ✅ refuses to demote/remove the only admin |
| `staff_roles` still RLS-locked (reached only via admin_* functions) | ✅ |

## Production cut-over
_(to complete at deployment)_
- Prod password toggles confirmed on: __________
- Admin functions (0003) applied to prod on: __________
- Merged to main / redeployed on: __________
- Live verification (admin sees panel; safe add+remove; non-admin does not): __________
