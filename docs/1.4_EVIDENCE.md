# Package 1.4 — evidence

## Dev test plan
1. Run `supabase/VALIDATE_INTEGRITY.sql` on dev → expect all zeros.
2. Run `supabase/migrations/0004_integrity.sql` on dev → "0004_integrity complete".
3. Booking still works: create a normal campaign → saves fine (valid data passes).
4. Delete-guard: try to delete a campaign that has an issued invoice → **blocked**
   with the clear message. Delete one with no invoice → **works**.
   (In the dev seed: TST-0003 has a Paid invoice, TST-0001 an Unpaid one — both
   should refuse; TST-0004/TST-0005 have none — should delete.)

## Dev verification — PASS (11 Aug 2026)
- ✅ VALIDATE returned all zeros on dev
- ✅ 0004 applied cleanly ("0004_integrity complete")
- ✅ Delete-guard: TST-0003 (has Paid invoice) refused with the clear message;
  TST-0004 (no invoice) deleted successfully

## Production cut-over — COMPLETE (11 Aug 2026)
- ✅ VALIDATE_INTEGRITY on prod returned all zeros (real data clean)
- ✅ Constraints applied on prod ("constraints applied")
- ✅ Delete-guard installed on prod ("delete guard installed")
- Applied in two blocks (A: constraints, B: trigger) to avoid a paste-truncation
  issue with the combined script. App unchanged — DB constraints only.

Package 1.4 complete.
